{
  "name": "aegisstadium-ai-monorepo",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend && npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "vercel-build": "npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend"
  }
}

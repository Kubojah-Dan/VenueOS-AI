{
  "name": "venueos-ai-monorepo",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
